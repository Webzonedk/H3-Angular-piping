import { Md5ConverterPipe } from './md5-converter.pipe';

describe('Md5ConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new Md5ConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
